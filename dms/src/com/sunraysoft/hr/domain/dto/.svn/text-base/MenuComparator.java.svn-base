package com.sunraysoft.hr.domain.dto;

import java.util.Comparator;

public class MenuComparator<T extends MenuDto> implements Comparator<T> {

	@Override
	public int compare(MenuDto x, MenuDto y) {
		return x.getPosition().compareTo(y.getPosition());
	}
	
}
